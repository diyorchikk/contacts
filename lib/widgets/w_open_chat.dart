import 'package:flutter/material.dart';

class WOpenChat extends StatelessWidget {
  const WOpenChat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
